<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.example.lianxi0325.mapper.UserMapper">
    <select id="selectUser" parameterType="java.lang.String" resultType="com.example.lianxi0325.entity.User">
        select * from user where username=#{username}
    </select>

    <insert id="insert" parameterType="com.example.lianxi0325.entity.User">
        insert into user (username,password,salt)values (#{username},#{password},#{salt})
    </insert>

    <update id="updatePassword" parameterType="com.example.lianxi0325.entity.User">
        update user set password=#{password},salt=#{salt} where id=#{id}
    </update>
 </mapper>